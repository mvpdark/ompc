# 敏感信息扫描报告

**扫描目标**: `C:\TRAE\OMPC-ZSCJ`
**扫描时间**: 2026-07-13
**扫描方法**: git ls-files 获取追踪文件列表 + 正则匹配敏感模式

---

## 一、扫描概览

| 指标 | 数值 |
|------|------|
| git 追踪文件总数 | 724 |
| 已扫描文本文件 | 117 |
| 跳过二进制文件 | 156 |
| 跳过 .env.example 模板 | 1 |
| 发现疑似敏感信息 | 1 处 |

---

## 二、检查模式清单

以下模式均已检查，结果如下：

### 1. 真实 API Key（前缀匹配）

| 模式 | 说明 | 结果 |
|------|------|------|
| `sk-[A-Za-z0-9]{20,}` | OpenAI / 通用 sk- 开头 API Key | **未发现** |
| `sk-ant-...` | Anthropic Claude API Key | **未发现** |
| `sk-proj-...` | OpenAI Project Key | **未发现** |
| `ghp_[A-Za-z0-9]{36}` | GitHub Personal Access Token | **未发现** |
| `gho_[A-Za-z0-9]{36}` | GitHub OAuth Token | **未发现** |
| `ghs_[A-Za-z0-9]{36}` | GitHub App Token | **未发现** |
| `ghr_[A-Za-z0-9]{76}` | GitHub Refresh Token | **未发现** |
| `github_pat_...` | GitHub Fine-grained PAT | **未发现** |
| `tvly-[A-Za-z0-9]{20,}` | Tavily API Key | **未发现** |
| `AIza[0-9A-Za-z\-_]{35}` | Google API Key | **未发现** |
| `AKIA[0-9A-Z]{16}` | AWS Access Key ID | **未发现** |
| `xox[baprs]-...` | Slack Token | **未发现** |
| `sk_live_/pk_live_...` | Stripe Key | **未发现** |
| `\d{9,10}:AA...` | Telegram Bot Token | **未发现** |
| `eyJ...` | JWT Token (已签发的) | **未发现** |

### 2. 硬编码密码 / Secret / JWT_SECRET_KEY

| 模式 | 说明 | 结果 |
|------|------|------|
| `PASSWORD = "..."` 等赋值 | Python 文件中硬编码密码 | **发现 1 处**（见下文） |
| `JWT_SECRET_KEY = "..."` | JWT 密钥硬编码 | **未发现**（.env.example 中为占位符） |
| `os.getenv('KEY', '默认值')` | 环境变量带硬编码默认值 | **未发现** |
| `BEGIN PRIVATE KEY` | 私钥文件 | **未发现** |
| `Bearer xxx` | 硬编码 Bearer Token | **未发现** |
| `(postgres\|mysql\|mongodb\|redis)://user:pass@` | 连接字符串内嵌密码 | **未发现** |

### 3. .env 文件检查

| 文件 | 状态 | 说明 |
|------|------|------|
| `backend/.env.example` | **已排除**（模板文件） | 仅含占位符，安全 |
| `.env`（真实环境文件） | **不存在** | git 未追踪任何 .env 文件 |

`.env.example` 中的占位符值（均为安全占位符，非真实密钥）：
- `JWT_SECRET_KEY=replace-with-a-long-random-secret`（占位符）
- `EMBEDDING_API_KEY=`（空值）
- `EXTRACT_API_KEY=`（空值）

### 4. Python 文件硬编码 API Key

对所有 git 追踪的 `.py` 文件进行了逐一扫描，**未发现**硬编码的真实 API Key。
仅发现 1 处测试用硬编码密码（见下文）。

### 5. 其他文件类型检查

| 文件类型 | 模式 | 结果 |
|----------|------|------|
| HTML/JS/TS/JSON/YAML 等 | password/secret/token 赋值 | **未发现真实密钥**（index.html 中匹配均为表单字段名） |

---

## 三、发现的敏感信息详情

### 发现 #1：测试文件中的硬编码密码

- **文件**: `backend/tests/test_auth.py`
- **行号**: 第 8 行
- **类型**: 硬编码密码赋值
- **变量名**: `VALID_PASSWORD`
- **值**: `testpassword123`
- **上下文代码**:
  ```python
  VALID_PHONE = "13800000001"
  VALID_PASSWORD = "testpassword123"
  VALID_NICKNAME = "测试用户"
  ```
- **风险评估**: **低风险**
  - 这是认证接口单元测试中使用的测试固件（test fixture）密码
  - 仅用于 `TestClient` 模拟注册/登录流程，不涉及任何真实系统
  - 同文件中还有 `"wrongpassword999"`（第 69 行）用于测试登录失败场景
  - 不构成真实凭证泄露，但属于硬编码测试数据

- **建议**: 如果团队有严格的代码规范要求，可考虑将测试密码移至 `conftest.py` 或测试配置文件中统一管理，但当前做法在测试代码中是常见且可接受的。

---

## 四、总结

### 扫描结论

`C:\TRAE\OMPC-ZSCJ` 仓库在敏感信息管理方面**整体安全**：

1. **未发现任何真实的 API Key**（无 sk-、ghp_、gho_、tvly- 等前缀的真实密钥）
2. **未发现任何真实的 JWT_SECRET_KEY**（.env.example 中为占位符，无硬编码）
3. **未追踪任何 .env 文件**（仅追踪了 .env.example 模板，含占位符）
4. **未发现私钥、Bearer Token、连接字符串内嵌密码**等其他敏感信息
5. **唯一发现**：`backend/tests/test_auth.py` 中的测试固件密码 `testpassword123`，属于测试数据，非真实生产凭证，风险极低

### 安全实践评价

该仓库的敏感信息管理实践良好：
- 使用 `.env.example` 模板 + 环境变量方式管理密钥
- `.env` 真实文件未被 git 追踪
- API Key 通过 `EMBEDDING_API_KEY`、`EXTRACT_API_KEY` 等环境变量注入，代码中无硬编码
- JWT 密钥使用占位符标注"生产环境必须替换"
